import React, { Component } from 'react';

class Clustering extends Component {
    render() {
        return (
            <div>
                Clustering
            </div>
        );
    }
}

export default Clustering;